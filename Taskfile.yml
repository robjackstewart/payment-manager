version: '3'

includes:
  backend:
    taskfile: backend/Taskfile.yml
    dir: backend
  frontend:
    taskfile: frontend/Taskfile.yml
    dir: frontend

tasks:
  environment:check:
    deps:
      - task: backend:environment:check

  build:
    deps:
      - task: backend:build
      - task: frontend:build

  test:
    deps:
      - task: backend:test